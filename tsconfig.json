{
  "compilerOptions": {
    // No `baseUrl`: it was removed in TypeScript 7, and `paths` is already
    // resolved relative to this file, so the alias works without it.
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
